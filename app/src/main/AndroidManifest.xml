<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edojs.etecapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ElegirCentroDistribucionActivity" />
        <activity android:name=".CarritoDeComprasActivity" />
        <activity android:name=".PagoDeProductosActivity" />
        <activity android:name=".VisualizarEstadoActivity" />
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MostrarTipodeProductoActivity" />
        <activity android:name=".GetProductosActivity" />
        <activity android:name=".AlgoritmosDeOrdenamientoActivity" />
        <activity android:name=".BuscarProductoActivity" />
        <activity android:name=".ElegirTipoDeOrdenamientoYPeticionActivity" />
        <activity android:name=".DirIpActivity" />
        <activity android:name=".BuscarYmostrarListaActivity"></activity>
    </application>

</manifest>